<Window x:Class="Wpf3DTutorial.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="WPF 3D Tutorial - 확장판" Width="900" Height="700"
        Background="#1E1E2E">

    <Grid>
        <!-- 조작 안내 -->
        <!-- Operation guide -->
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Top"
                    Margin="0,8,0,0">
            <TextBlock Text="좌클릭 드래그: 회전 | 우클릭 드래그: 패닝 | 휠: 확대/축소"
                       Foreground="#CDD6F4" FontSize="13"
                       HorizontalAlignment="Center"/>
        </StackPanel>

        <!-- 자동 회전 토글 버튼 -->
        <!-- Auto-rotation toggle button -->
        <ToggleButton x:Name="btnAutoRotate"
                      Content="자동 회전"
                      HorizontalAlignment="Left" VerticalAlignment="Top"
                      Margin="12,8,0,0" Padding="10,4"
                      Checked="AutoRotate_Checked"
                      Unchecked="AutoRotate_Unchecked"/>

        <Viewport3D x:Name="viewport"
                     MouseWheel="Viewport_MouseWheel"
                     MouseLeftButtonDown="Viewport_MouseLeftButtonDown"
                     MouseLeftButtonUp="Viewport_MouseLeftButtonUp"
                     MouseRightButtonDown="Viewport_MouseRightButtonDown"
                     MouseRightButtonUp="Viewport_MouseRightButtonUp"
                     MouseMove="Viewport_MouseMove">

            <!-- 카메라 -->
            <!-- Camera -->
            <Viewport3D.Camera>
                <PerspectiveCamera x:Name="camera"
                                   Position="5,4,6"
                                   LookDirection="-5,-4,-6"
                                   UpDirection="0,1,0"
                                   FieldOfView="45"/>
            </Viewport3D.Camera>

            <!-- 조명 -->
            <!-- Lighting -->
            <ModelVisual3D>
                <ModelVisual3D.Content>
                    <Model3DGroup>
                        <DirectionalLight Color="White" Direction="-1,-1,-1"/>
                        <DirectionalLight Color="#666666" Direction="1,0.5,0.5"/>
                        <AmbientLight Color="#404040"/>
                    </Model3DGroup>
                </ModelVisual3D.Content>
            </ModelVisual3D>

            <!-- 3D 오브젝트들 (코드에서 추가) -->
            <!-- 3D objects (added from code-behind) -->
            <ModelVisual3D x:Name="sceneVisual"/>

        </Viewport3D>
    </Grid>
</Window>
